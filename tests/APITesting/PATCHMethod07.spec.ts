import {test, expect} from '@playwright/test'
import patchRequestBody from '../../testdata/patch_request_body.json'

test('Verifying Patch request',({request})=>
{
const currentTestBody = structuredClone(patchRequestBody); 


})

